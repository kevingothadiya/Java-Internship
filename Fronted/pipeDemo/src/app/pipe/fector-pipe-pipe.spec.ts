import { FectorPipePipe } from './fector-pipe-pipe';

describe('FectorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FectorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
